- name: Hello World Sample
  hosts: all
  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World!"

    - name: Print Env Var
      debug:
        msg: "{{ lookup('env','USER','HOME','SHELL') }}"
        
